import { RouteComponentProps } from "@reach/router";
import { LoginContainer } from "src/containers/Login/LoginContainer";

export const LoginRoute = ({}: RouteComponentProps) => {
  return <LoginContainer />;
};
